import 'package:flutter/material.dart';

class WordScrambleGame extends StatelessWidget {
  const WordScrambleGame({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Word Scramble")),
      body: const Center(child: Text("Word Scramble Coming Soon!")),
    );
  }
}
